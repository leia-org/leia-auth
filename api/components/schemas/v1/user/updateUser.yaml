type: object
properties:
  email:
    type: string
    format: email
    example: new-email@leia.local
  password:
    type: string
    format: password
    example: new-secret-password
  role:
    $ref: './role.yaml'
  useSystemApiKey:
    type: boolean
    example: false
minProperties: 1
