type: object
properties:
  currentPassword:
    type: string
    format: password
    example: current-secret-password
  newPassword:
    type: string
    format: password
    minLength: 6
    example: new-secret-password
required:
  - currentPassword
  - newPassword
